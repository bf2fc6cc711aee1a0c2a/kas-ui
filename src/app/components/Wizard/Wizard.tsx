import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Wizard: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">Wizard goes here</Title>
  </PageSection>
)

export { Wizard };
