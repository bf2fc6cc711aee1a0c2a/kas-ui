import React from 'react';
import './Loading.css';
import { Spinner } from '@redhat-cloud-services/frontend-components/components/esm/spinner';

export const Loading = () => {
  return (
   <Spinner/>
  )
};
